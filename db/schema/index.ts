/**
 * Table-schema barrel.
 *
 * Ticket 02 adds the `evidence` and `evidence_audit_events` tables here. For the
 * skeleton (ticket 01) this file exists so the Drizzle client and migration
 * tooling have a stable import target.
 */
export {};
